{
  "buildCommand": "npm install && cd ui && npm install && npm run build && cd .. && cp -r ui/.next .next && (cp -r ui/public public 2>/dev/null || true) && cp ui/next.config.mjs next.config.mjs && cp ui/package.json package.json",
  "installCommand": "npm install"
}
